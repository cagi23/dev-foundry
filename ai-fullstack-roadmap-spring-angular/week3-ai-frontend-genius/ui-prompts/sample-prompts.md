# Sample UI Prompts for Claude / ChatGPT

1. "Generate an Angular component that displays products in a responsive grid with cards (name, category, price)."
2. "Optimize this component for Angular standalone components and lazy loading."
3. "Generate a service for calling http://localhost:8081/api/products with filter by category."
